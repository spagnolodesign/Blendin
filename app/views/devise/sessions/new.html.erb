<section class="Auth clearfix mt4 mxn2 py2 px2">
  <div class="sm-col-6 lg-col-3 mx-auto">
    <%= render "devise/shared/headline" %>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
      <%= f.email_field :email, autofocus: true, class:"Input", placeholder:"Email" %>
      <%= f.password_field :password, autocomplete: "off", class:"Input", placeholder:"Password" %>

      <% if devise_mapping.rememberable? -%>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end -%>

      <div class="actions">
        <%= f.submit "Sign in", class: "Button CenterAling" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</section>
